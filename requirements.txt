# ULTRA-LOW LATENCY Voxtral + Kokoro TTS Requirements
# Optimized for RunPod deployment with <500ms end-to-end latency
# Verified stable versions for production use
# Installation: pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121

# Core PyTorch Stack (CUDA 12.1 compatible - RunPod Verified)
torch==2.4.1+cu121
torchaudio==2.4.1+cu121
torchvision==0.19.1+cu121

# Transformers with Voxtral support (Production stable)
transformers==4.45.2
huggingface-hub==0.25.1
accelerate==0.24.1
tokenizers==0.15.0

# Mistral Common with Audio Support (Stable)
mistral-common[audio]==1.8.1

# Kokoro TTS (Stable version)
kokoro==0.9.4

# Audio Processing (RunPod optimized versions)
librosa==0.10.1
soundfile==0.12.1
numpy==1.24.4
scipy==1.11.4

# Web Framework (Stable versions for RunPod compatibility)
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0
pydantic==2.5.3
pydantic-settings==2.1.0
python-multipart==0.0.6
aiofiles==23.2.1
httpx==0.25.2

# Audio processing and system utilities (Stable versions)
pyaudio==0.2.14
pillow==10.1.0

# Configuration and utilities (Stable versions)
pyyaml==6.0.1
python-dotenv==1.0.0
psutil==5.9.6

# Development and testing (Optional - removed for production)
# pytest==8.3.4
# pytest-asyncio==0.24.0

