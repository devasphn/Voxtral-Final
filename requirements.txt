# ULTRA-LOW LATENCY Voxtral + Kokoro TTS Requirements
# Optimized for RunPod deployment with <500ms end-to-end latency
# Verified stable versions for production use
# Installation:
#   1. pip install torch==2.4.1+cu121 torchaudio==2.4.1+cu121 torchvision==0.19.1+cu121 --extra-index-url https://download.pytorch.org/whl/cu121
#   2. pip install -r requirements.txt

# NOTE: PyTorch packages are installed separately via command line for CUDA 12.1 compatibility
# torch==2.4.1+cu121 (install manually)
# torchaudio==2.4.1+cu121 (install manually)
# torchvision==0.19.1+cu121 (install manually)

# Transformers with Voxtral support (Requires 4.56.0+ for Voxtral model compatibility)
transformers>=4.56.0
huggingface-hub>=0.26.0
accelerate>=0.25.0
tokenizers>=0.20.0

# Mistral Common with Audio Support (Compatible with transformers>=4.56.0)
mistral-common[audio]>=1.8.1

# Kokoro TTS (Stable version)
kokoro>=0.9.4

# Audio Processing (Updated for transformers>=4.56.0 compatibility)
librosa>=0.10.1
soundfile>=0.12.1
numpy>=1.24.4,<2.0.0
scipy>=1.11.4

# Web Framework (Updated for transformers>=4.56.0 compatibility)
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
websockets>=12.0
pydantic>=2.5.3
pydantic-settings>=2.1.0
python-multipart>=0.0.6
aiofiles>=23.2.1
httpx>=0.25.2

# System utilities (pyaudio removed for RunPod compatibility)
pillow>=10.1.0

# Configuration and utilities (Compatible versions)
pyyaml>=6.0.1
python-dotenv>=1.0.0
psutil>=5.9.6

# Development and testing (Optional - removed for production)
# pytest==8.3.4
# pytest-asyncio==0.24.0

